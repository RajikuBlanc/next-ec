# NextProjectBase

## プロジェクトの作成 & git の初期化

```bash
npx create-next-app next-snipcart-ecsite --typescript
cd next-snipcart-ecsite
rm -rf .git
git init
git checkout -b main
git add .
git commit -m 'first-commit'
```

### リポジトリの作成

```bash
gh create repo
```
